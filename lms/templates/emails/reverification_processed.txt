<%namespace file="../main.html" import="stanford_theme_enabled" />
<%! from django.utils.translation import ugettext as _ %>
% if status == "approved":
    ${_("Your verification for course {course_name} and assessment {assessment} "
        "has been passed."
    ).format(course_name=course_name, assessment=assessment)}

    ${_("Click on below link to return to the courseware")}
     % if is_secure:
      https://${ site }${ courseware_url }
    % else:
      http://${ site }${ courseware_url }
    % endif
% endif

${_("The {platform_name} Team").format(platform_name=settings.PLATFORM_NAME)}
